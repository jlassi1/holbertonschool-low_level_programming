#include <stdio.h>

/**
 * main - main function
 * Description:prints the alphabet in lowercase
 * Return: void
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
		putchar('\n');
	return (0);
}
