#include "holberton.h"

/**
 * _abs - Write a function that computes the absolute value of an integer.
 *@n : integer
 * Return:1 else return:0
 */
int _abs(int n)
{

if (n > 0)
return (n);

else
return (-n);

}
